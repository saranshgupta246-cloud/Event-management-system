# Backend API base (with /api). Used by axios for all /api/* requests.
VITE_API_URL=http://localhost:5000/api

# Backend base URL (no /api). Used by Socket.IO chat.
VITE_API_BASE_URL=http://localhost:5000
